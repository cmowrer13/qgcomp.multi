% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_mixture_data.R
\name{sim_mixture_data}
\alias{sim_mixture_data}
\title{Generate simulated dataset with two exposure mixtures}
\usage{
sim_mixture_data(
  n,
  pA,
  pB,
  rho_within_A,
  rho_within_B,
  rho_between,
  psi1,
  psi2,
  psi12,
  sigma_eps = 1,
  q = 4,
  return_quantized = FALSE,
  seed = NULL
)
}
\arguments{
\item{n}{Number of observations.}

\item{pA}{Number of components in mixture A.}

\item{pB}{Number of components in mixture B.}

\item{rho_within_A}{Correlation between components within mixture A.}

\item{rho_within_B}{Correlation between components within mixture B.}

\item{rho_between}{Correlation between components across mixtures.}

\item{psi1}{Effect of a one-quantile increase in mixture A.}

\item{psi2}{Effect of a one-quantile increase in mixture B.}

\item{psi12}{Interaction effect representing the change in the effect of
mixture A when mixture B increases by one quantile.}

\item{sigma_eps}{Standard deviation of the outcome error term.}

\item{q}{Number of quantiles used to discretize exposures.}

\item{return_quantized}{Logical; if TRUE returns quantized exposures,
otherwise returns the original continuous exposures.}

\item{seed}{Optional random seed for reproducibility.}
}
\value{
A data frame containing the simulated outcome \code{Y}, mixture
exposures (\code{X1...XpA} and \code{W1...WpB}), and covariate \code{C}.
}
\description{
Generates a dataset for simulation studies involving two mixtures of
correlated exposures and their interaction. Exposures are generated from
a multivariate normal distribution with user-specified within-mixture and
between-mixture correlations, then discretized into quantiles. The outcome
is generated from a model where the effects correspond to simultaneous
one-quantile increases in each mixture.
}
\details{
The data-generating model is:

Y = psi1 * S_A + psi2 * S_B + psi12 * S_A * S_B + C + error

where S_A and S_B are the average quantile levels of the exposures in
mixture A and mixture B, respectively.

Continuous exposures are generated from a multivariate normal distribution
with a block correlation structure. Exposures are then discretized into
quantile categories ranging from 0 to q-1. The outcome is generated using
the quantized exposures so that the parameters psi1, psi2, and psi12
correspond directly to the estimands in two-mixture quantile
g-computation models.
}
\examples{
dat <- sim_mixture_data(
  n = 1000,
  pA = 4,
  pB = 4,
  rho_within_A = 0.5,
  rho_within_B = 0.5,
  rho_between = 0.3,
  psi1 = 0.5,
  psi2 = 0.3,
  psi12 = 0.2,
  return_quantized = TRUE,
  seed = 123
)

}
