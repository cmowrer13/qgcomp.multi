% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coef.qgcompmulti_mi.R
\name{qgcompmulti-mi-extractors}
\alias{qgcompmulti-mi-extractors}
\alias{coef.qgcompmulti_mi}
\alias{vcov.qgcompmulti_mi}
\alias{confint.qgcompmulti_mi}
\title{Core extractor methods for pooled qgcompmulti multiple-imputation objects}
\usage{
\method{coef}{qgcompmulti_mi}(object, ...)

\method{vcov}{qgcompmulti_mi}(object, ...)

\method{confint}{qgcompmulti_mi}(object, parm = NULL, level = 0.95, method = NULL, ...)
}
\arguments{
\item{object}{A fitted \code{qgcompmulti_mi} object.}

\item{...}{Not used.}

\item{parm}{Optional specification of which coefficients to include. May be
\code{NULL} for all coefficients, an integer vector of coefficient positions, or
a character vector of coefficient names.}

\item{level}{Confidence level for the returned intervals. Must be strictly
between 0 and 1.}

\item{method}{Optional interval method. Pooled multiple-imputation
coefficient reporting supports only \code{"wald"} in Version 0.5.0. \code{NULL} uses
the fitted object's stored default, which is \code{"wald"} for pooled MI fits.}
}
\value{
\code{coef()} returns a named numeric vector of pooled MSM coefficients on the
fitting scale.

\code{vcov()} returns the pooled covariance matrix aligned with \code{coef(object)}.

\code{confint()} returns pooled Wald-style confidence intervals. The Wald
calculation uses Rubin-pooled coefficients, standard errors, and
term-specific degrees of freedom on the fitting scale. For odds-ratio and
rate-ratio estimands, returned interval limits are exponentiated for display.
}
\description{
Extract the pooled marginal structural model (MSM) coefficient results from a
fitted \code{qgcompmulti_mi} object using standard R generics.
}
\details{
These extractors operate on the pooled multiple-imputation result, not on
the individual completed-data fits. They therefore return Rubin-pooled MSM
coefficient summaries for the inferential target defined by
\code{\link[=qgcomp.glm.multi.mi]{qgcomp.glm.multi.mi()}}, rather than per-imputation coefficient tables,
pooled prediction objects, or pooled diagnostics. If you need to inspect the
stored completed-data fits directly, fit with \code{keep_fits = TRUE} and extract
the individual \code{"qgcompmulti"} objects from \code{object$fits$imputation_fits}.
}
\seealso{
\code{\link[=summary.qgcompmulti_mi]{summary.qgcompmulti_mi()}}, \code{\link[=print.qgcompmulti_mi]{print.qgcompmulti_mi()}},
\code{\link[broom:reexports]{broom::tidy()}}, \code{\link[broom:reexports]{broom::glance()}}, \code{\link[=qgcomp.glm.multi.mi]{qgcomp.glm.multi.mi()}}
}
