% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qgcompmulti_msm_fit.R
\name{qgcompmulti_msm_fit}
\alias{qgcompmulti_msm_fit}
\title{Fit the marginal structural model for two-mixture quantile g-computation}
\usage{
qgcompmulti_msm_fit(
  f,
  data,
  mix1,
  mix2,
  interaction = TRUE,
  family = gaussian(),
  q = 4,
  centering = "none",
  id = NULL,
  MCsize = nrow(data),
  seed = NULL
)
}
\arguments{
\item{f}{A model formula for the outcome regression. The formula should
include the outcome and any baseline covariates. Mixture variables listed
in \code{mix1} and \code{mix2} should also appear in the formula if they are to be
included in the outcome model.}

\item{data}{A data frame containing the outcome, exposure variables, and
any covariates in the model.}

\item{mix1}{A character vector giving the names of the variables in the
first exposure mixture.}

\item{mix2}{A character vector giving the names of the variables in the
second exposure mixture.}

\item{interaction}{Logical; if \code{TRUE}, includes an interaction term between
the two mixture indices in the marginal structural model. If \code{FALSE}, only
main effects are estimated.}

\item{family}{A GLM family object (e.g., \code{gaussian()}, \code{binomial()},
\code{poisson()}) specifying the outcome model.}

\item{q}{Integer greater than or equal to 2 giving the number of quantiles
used to discretize the exposure variables and define the intervention grid,
or \code{NULL} to skip quantization and define a 3 x 3 intervention grid using
pooled 25th, 50th, and 75th percentiles for each mixture.}

\item{centering}{Character string controlling how the marginal structural
model intervention variables are coded when \code{q = NULL}. Must be one of \code{"none"}
or \code{"median"}. Centering affects only the MSM predictors and does not change
the outcome regression fit. This argument is ignored when \code{q} is numeric.}

\item{id}{Optional character string giving the name of a cluster identifier
variable. If supplied, Monte Carlo subsampling is performed at the cluster
level rather than the observation level.}

\item{MCsize}{Optional integer controlling the Monte Carlo sample size used
to approximate the marginalization step in g-computation. If equal to
\code{nrow(data)}, all observations are used. Smaller values compute predicted
outcomes over a random subsample drawn from the empirical distribution. When
\code{id} is supplied and \code{MCsize < nrow(data)}, the approximation is implemented
by sampling \code{MCsize} clusters with replacement.}

\item{seed}{Optional integer random seed used to make Monte Carlo
subsampling reproducible when \code{MCsize < nrow(data)}. If \code{NULL}, the current
RNG state is used and not modified by \code{qgcompmulti_msm_fit()}.}
}
\value{
A list with components:
\describe{
\item{outcome_fit}{The fitted outcome regression model object.}
\item{msm_fit}{The fitted marginal structural model object.}
\item{coefficients}{A names vector of MSM coefficients}
\item{n_used}{The number of observations used in the MSM pseudo-dataset
prediction step.}
}
}
\description{
Fits the outcome regression, computes predicted potential outcomes under
joint interventions on two exposure mixtures, and estimates the marginal
structural model (MSM) coefficients that summarize the resulting
two-dimensional dose-response surface. This function is primarily an
internal helper used by \code{\link[=qgcomp.glm.multi]{qgcomp.glm.multi()}} and will not typically
need to be called directly by users.
}
\details{
Given a fitted outcome model, the function evaluates predicted outcomes over
either a grid of uniform mixture quantiles or, when \code{q = NULL}, a 3 x 3 grid
formed from the pooled 25th, 50th, and 75th percentiles of each mixture, and
regresses these predictions on the mixture intervention levels to obtain MSM
parameters. When \code{interaction = TRUE}, the fitted MSM has the form

\deqn{
E[Y^{x(q1), w(q2)}] = \psi_1 q_1 + \psi_2 q_2 + \psi_{12} q_1 q_2
}

where \code{psi1} and \code{psi2} represent the main effects of increasing the
intervention level for mixtures 1 and 2 by one unit on the MSM scale, and
\code{psi12} represents their interaction on that same scale. For quantized
analyses this is a one-quantile increase; for \code{q = NULL} it is a one-unit
increase in the original-scale intervention coding.

When \code{q = NULL} and \code{centering = "median"}, the MSM is fit using intervention
values centered at the pooled median of each mixture.

This function carries out the core g-computation step for the two-mixture
extension. It first fits a generalized linear model for the outcome using
\code{glm()}. It then constructs a grid of intervention levels over the two
mixtures, replaces the observed mixture values with the intervention values,
and computes predicted outcomes for each observation (or for a Monte Carlo
subsample if \code{MCsize < nrow(data)}). These predicted outcomes are stacked
into a pseudo-dataset and used to fit a marginal structural model that
summarizes the dose-response surface. Note that predicted potential
outcomes are computed on the response scale and the marginal
structural model is fit using an identity link, regardless of the outcome
model.

When \code{q} is an integer, the intervention grid is \verb{0, 1, ..., q - 1} for each
mixture, corresponding to simultaneous quantile increases in all components
of that mixture.

When \code{q = NULL}, each mixture is instead set to common pooled percentile
values on the original exposure scale. The resulting MSM coefficients are
therefore scale-dependent and should be interpreted in the units of the
underlying exposures. If \code{centering = "median"}, the intercept corresponds
to the pooled-median intervention for both mixtures.

Because the number of intervention combinations grows as \code{q^2}, this step
can become computationally expensive for large datasets. The \code{MCsize}
argument reduces this burden by approximating the empirical covariate
distribution using a random subset while leaving the outcome model fit
unchanged.
}
\examples{
dat <- sim_mixture_data(
  n = 500,
  pA = 3,
  pB = 3,
  rho_within_A = 0.3,
  rho_within_B = 0.3,
  rho_between = 0.2,
  psi1 = 0.5,
  psi2 = 0.3,
  psi12 = 0.2,
  return_quantized = FALSE,
  seed = 123
)

qgcompmulti_msm_fit(
  f = Y ~ X1 + X2 + X3 + W1 + W2 + W3 + C,
  data = dat,
  mix1 = c("X1", "X2", "X3"),
  mix2 = c("W1", "W2", "W3"),
  interaction = TRUE,
  q = 4,
  MCsize = nrow(dat),
  seed = 13
)

}
