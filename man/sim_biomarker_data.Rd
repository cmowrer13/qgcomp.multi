% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_biomarker_data.R
\name{sim_biomarker_data}
\alias{sim_biomarker_data}
\title{Simulate a biomarker dataset for two-mixture inflammation analyses}
\usage{
sim_biomarker_data(n = 800, include_log = FALSE, seed = NULL)
}
\arguments{
\item{n}{Number of observations to generate.}

\item{include_log}{Logical; if \code{TRUE}, also include log-transformed biomarker
variables named \code{ln_mep}, \code{ln_mibp}, \code{ln_mehpp}, \code{ln_bpa}, \code{ln_bp3}, and
\code{ln_ppb}.}

\item{seed}{Optional integer random seed. When supplied, the generated
dataset is reproducible and the caller's RNG state is restored on exit.}
}
\value{
A data frame containing \code{inflammation_score}, six raw biomarker
concentrations, and the covariates \code{age_years}, \code{bmi_kg_m2}, and
\code{smoker}. If \code{include_log = TRUE}, log-transformed biomarker variables are
included as well.
}
\description{
Generates a narrative environmental-health teaching dataset with two
correlated exposure mixtures, a continuous inflammation outcome, and a small
amount of intentional model imperfection. The simulated biomarkers are
positive, right-skewed concentrations that naturally motivate log
transformation before model fitting.
}
\details{
Mixture 1 represents urinary phthalate metabolites:
\itemize{
\item \code{mep_ng_ml}
\item \code{mibp_ng_ml}
\item \code{mehpp_ng_ml}
}

Mixture 2 represents urinary phenol biomarkers:
\itemize{
\item \code{bpa_ng_ml}
\item \code{bp3_ng_ml}
\item \code{ppb_ng_ml}
}

The outcome \code{inflammation_score} is generated from weighted mixture summaries
on the log-concentration scale, a moderate positive interaction between the
two mixtures, common epidemiologic covariates, and a mild quadratic term in
the phthalate mixture. This design makes the fitted MSM informative but not
unrealistically perfect.

This helper is intended for examples, documentation, and teaching. The
exposure concentrations are simulated as correlated log-normal variables with
moderate within-mixture correlation, mild between-mixture correlation, and
deliberately different raw scales across chemicals.

Those scale differences are useful for demonstrating:
\itemize{
\item standard quantized fitting,
\item original-scale fitting with \code{q = NULL},
\item why log transformation can be scientifically natural for biomarker
concentrations, and
\item why median centering can make the \code{q = NULL} intercept more
interpretable.
}
}
\examples{
dat <- sim_biomarker_data(n = 200, seed = 123)
head(dat)

dat_log <- sim_biomarker_data(n = 200, include_log = TRUE, seed = 123)
head(dat_log[c("ln_mep", "ln_bpa", "inflammation_score")])

}
