% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.qgcompmulti.R
\name{plot.qgcompmulti}
\alias{plot.qgcompmulti}
\title{Plot a qgcompmulti fit}
\usage{
\method{plot}{qgcompmulti}(
  x,
  style = c("heatmap", "contour"),
  grid = NULL,
  interval = FALSE,
  slice = NULL,
  level = 0.95,
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  ...
)
}
\arguments{
\item{x}{A fitted \code{"qgcompmulti"} object.}

\item{style}{Character string specifying the surface display style.
Supported values are \code{"heatmap"} and \code{"contour"}.}

\item{grid}{Optional user-specified MSM grid with columns \code{psi1} and \code{psi2}.
If omitted, the stored fit-time MSM grid is used.}

\item{interval}{Logical; if \code{TRUE}, produces a slice-based line display with
bootstrap percentile intervals instead of a 2D surface plot.}

\item{slice}{Optional list with elements \code{var} and \code{value} describing the
fixed intervention coordinate for interval plotting. For example,
\code{list(var = "psi2", value = 1)} fixes \code{psi2} and varies \code{psi1} over the
stored MSM grid support.}

\item{level}{Confidence level used when \code{interval = TRUE}.}

\item{xlab}{Optional x-axis label.}

\item{ylab}{Optional y-axis label.}

\item{main}{Optional plot title.}

\item{...}{Additional graphical parameters passed to the underlying base
graphics call.}
}
\value{
Invisibly returns the structured prediction result used to draw the
plot.
}
\description{
Produces a base-graphics display of the fitted marginal structural model
(MSM) surface from a \code{\link[=qgcomp.glm.multi]{qgcomp.glm.multi()}} fit. The default display is a
heatmap over the stored fit-time MSM grid. Contour plotting is also
supported, along with a slice-based interval display for MSM predictions.
}
